name: Halifax validation

on:
  workflow_dispatch:

jobs:
  validate:
    name: Validate Halifax runner
    runs-on: [self-hosted, "halifax-bl4ckbl1zz-dokploy-b14a8f16"]
    timeout-minutes: 10
    steps:
      - name: Verify disposable runner
        run: |
          test -n "${RUNNER_NAME}"
          echo "Halifax runner is ready"
